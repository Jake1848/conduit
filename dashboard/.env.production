# Production build target. The regtest sandbox API, exposed via nginx + TLS at
# api-test.conduit.energy. Vercel project env vars override this (see Step 7:
# switch to https://api.conduit.energy for testnet). NEXT_PUBLIC_* is non-secret.
NEXT_PUBLIC_API_URL=https://api-test.conduit.energy
